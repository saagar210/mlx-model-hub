# Knowledge Engine Configuration
# Copy to .env and customize

# ============================================================
# APPLICATION (defaults work out of the box)
# ============================================================
ENVIRONMENT=development
DEBUG=false
LOG_LEVEL=INFO

# ============================================================
# FREE TIER (default) - Ollama + self-hosted databases
# No API keys required! Just have Ollama running.
# ============================================================

# Ollama (FREE) - Make sure Ollama is running: ollama serve
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_RERANK_MODEL=qllama/bge-reranker-v2-m3
OLLAMA_LLM_MODEL=llama3.2

# Qdrant (FREE, self-hosted via Docker)
QDRANT_URL=http://localhost:6333

# PostgreSQL (FREE, self-hosted via Docker)
POSTGRES_PASSWORD=localdev
DATABASE_URL=postgresql+asyncpg://knowledge_engine:localdev@localhost:5432/knowledge_engine

# ============================================================
# OPTIONAL: Graph Database
# Enable with: GRAPH_ENABLED=true
# Start Neo4j with: docker compose --profile graph up -d
# ============================================================
GRAPH_ENABLED=false
NEO4J_PASSWORD=localdev

# ============================================================
# OPTIONAL: Premium AI Providers (better quality, costs $$$)
# Uncomment and set API keys to upgrade from Ollama
# ============================================================

# Voyage AI Embeddings (~$0.06 per 1M tokens)
# Get key at: https://www.voyageai.com/
# EMBEDDING_PROVIDER=voyage
# VOYAGE_API_KEY=your-voyage-api-key

# Cohere Reranking (~$2 per 1000 searches)
# Get key at: https://dashboard.cohere.com/
# RERANK_PROVIDER=cohere
# COHERE_API_KEY=your-cohere-api-key

# Anthropic LLM (for RAG generation)
# Get key at: https://console.anthropic.com/
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=your-anthropic-api-key

# ============================================================
# OPTIONAL: Redis Cache
# Start with: docker compose --profile cache up -d
# ============================================================
REDIS_ENABLED=false
